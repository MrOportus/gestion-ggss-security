// sw.js — redireccionado a firebase-messaging-sw.js
// Este archivo ya no se usa directamente. El SW principal es /firebase-messaging-sw.js
// que maneja tanto el caching como las notificaciones push de FCM.
// Se mantiene como fallback vacío por compatibilidad.
console.log('[sw.js] Este SW ya no está activo. El SW principal es firebase-messaging-sw.js');
